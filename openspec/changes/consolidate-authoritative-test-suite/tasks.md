## 1. Required spec and baseline

- [x] 1.1 Validate the active change registry, proposal, design, tasks and capability specs.
- [x] 1.2 Record the pre-consolidation local collection and duplicate-execution baseline.
- [x] 1.3 Add `make test-loop TEST=<nodeid>` with a hard 60-second fail-fast bound.
- [x] 1.4 Remove duplicate aggregate R0/R4 execution from the local canonical path.

## 2. Authoritative inventory and budget

- [x] 2.1 Record a KEEP, MERGE, REPLACE or DELETE disposition for retained contracts and obsolete wrappers.
- [x] 2.2 Maintain one exact-node manifest with a stable identifier and local domain.
- [x] 2.3 Reject a missing, duplicate, misplaced or unclassified test definition.
- [x] 2.4 Keep the inventory within the 180–220 target and below the hard cap of 250.
- [x] 2.5 Remove duplicate checker and policy wrappers after retaining their real owner.

## 3. Local validation policy

- [x] 3.1 Remove the #348-owned CI path-routing artifacts and record that CI is out of scope.
- [x] 3.2 Define docs/OpenSpec-only work as requiring no runtime test and package acceptance as manual/input-scoped.
- [x] 3.3 Validate each affected local domain through its authoritative runner with declared dev dependencies.

## 4. Fixture and consolidation implementation

- [x] 4.1 Reuse an isolated migrated DuckDB template only for compatible current-schema contracts.
- [x] 4.2 Keep a mutation-isolation contract for copied warehouses.
- [x] 4.3 Preserve dedicated fresh migration, idempotency, upgrade, rollback, crash, reopen, locking and multiprocessing inputs.
- [x] 4.4 Merge equivalent feature scenarios at their public boundary and retain distinct risk owners.
- [x] 4.5 Record financial, point-in-time, lineage, recovery, security and package-risk owners for each deletion.

## 5. Final local evidence and closure

- [x] 5.1 Freeze one final SHA; run strict OpenSpec validation, the bounded owning contract, relevant local domains and the complete authoritative suite once.
- [x] 5.2 Record final local count, duration and fixture/consolidation evidence on that SHA.
- [ ] 5.3 Obtain independent specification, code-quality, security and QA review; push a branch and open a PR referencing #348 without CI closure claims.
- [ ] 5.4 Merge the approved PR, confirm #348 closed, archive the OpenSpec and validate the archived state on `main`.
