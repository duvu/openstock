# Closed-loop clarification

The file-based observability change is intended to support a closed-loop AI repair process: runtime logs -> AI-readable bundle -> AI coding fix -> validation -> deploy or rollback -> new logs.
